# Included from the nginx http{} context.
# Keep these zones independent from the other services on the server.
limit_req_zone $binary_remote_addr zone=segundavida_api_read:10m rate=5r/s;
limit_req_zone $binary_remote_addr zone=segundavida_api_write:10m rate=2r/s;
